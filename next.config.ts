import { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Add any specific settings you had here, like:
  // experimental: {
  //   serverActions: true,
  // },
};

export default nextConfig;